select
	sum(distinct c_integer) as a1,
	avg(distinct c_integer) as a2,
	sum(distinct c_integer) as a3,
	avg(distinct c_integer) as a4,
	sum(distinct c_integer) as a5,
	avg(distinct c_integer) as a6,
	sum(distinct c_integer) as a7,
	avg(distinct c_integer) as a8,
	sum(distinct c_integer) as a9,
	avg(distinct c_integer) as a10
from
alltypes
;

