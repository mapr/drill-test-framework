{
    "testId": "aggregation_sanity",
    "type": "group",
    "description": "Sanity tests for aggregation",
    "categories": [ "functional" ],
    "matrices": [
        {
            "query-file": ".*.sql",
            "schema": "dfs_test.aggregation",
            "output-format": "tsv",
            "expected-file": ".*.res",
            "verification-type": [
                "in-memory"
            ]
        }
    ],
    "datasources": [
        {
            "mode": "cp",
            "src": "Datasources/aggregation",
            "dest": "aggregation"
        }
    ]
}

