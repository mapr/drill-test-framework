{
    "testId": "schema-changes",
    "type": "group",
    "description": "Test p1tests function queries via jdbc",
    "categories": [
        "functional"
    ],
    "matrices": [
        {
            "query-file": ".*.q",
            "schema": "dfs_test.drillTestDirP1",
            "output-format": "tsv",
            "expected-file": ".*.e_tsv",
            "verification-type": [
                "in-memory"
            ]
        }
    ],
    "datasources": [
      {
         "mode": "cp",
         "src": "Datasources/schema-changes",
         "dest": "schema-changes"
      } 
    ]
}
