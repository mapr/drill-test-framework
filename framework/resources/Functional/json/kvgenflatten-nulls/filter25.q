select * from (select s1.type type, flatten(s1.rms.rptd) rptds from (select d.type type, flatten(d.map.rm) rms from `data.json` d) s1 where s1.rms.mapid='m1') s2 where s2.rptds.c < 0 and s2.type='store';
