explain plan for select * from ( select concat(student_id, '_ABC'), concat(name, '_A'), concat(age, '_D'), concat(gpa, '_B'), concat(studentnum, '_C'), concat(create_time, '_123') from student_parquet_v where student_id=10 ) t limit 0;
