explain plan for select * from ( select concat(voter_id, '_A'), concat(name, '_B'), concat(age, '_C'), concat(registration, '_D'), concat(contributions, '_E'),concat(isVote, '_F') from voter_parquet_v where voter_id=10 ) t limit 0;
