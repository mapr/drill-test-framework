explain plan for select * from (
SELECT AVG(col_int) OVER (PARTITION BY col_dt) avg_int, col_dt, col_int FROM `smlTbl_v` WHERE col_vchar_52 = 'AXXXXXXXXXXXXXXXXXXXXXXXXXCXXXXXXXXXXXXXXXXXXXXXXXXB') t limit 0;