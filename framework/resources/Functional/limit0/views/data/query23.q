create or replace view v23 as select cast(rankings.pageRank as int) pageRank, cast(rankings.pageURL as varchar(100)) pageURL, cast(rankings.avgDuration as integer) avgDuration,cast(uservisits.sourceIP as char(15)) sourceIP, cast(uservisits.destinationURL as character varying(100)) destinationURL, cast(uservisits.visitDate as date) visitDate, cast(uservisits.adRevenue as float) adRevenue, cast(uservisits.userAgent as varchar(61)) userAgent, cast(uservisits.countryCode as char(3)) countryCode, cast(uservisits.languageCode as varchar(6)) languageCode, cast(uservisits.searchWord as varchar(23)) searchWord, cast(uservisits.duration as int) duration from `dfs/parquet/rankings` rankings inner join `dfs/parquet/uservisits` uservisits on rankings.pageURL = uservisits.destinationURL; 
select * from v23;
drop view v23;
