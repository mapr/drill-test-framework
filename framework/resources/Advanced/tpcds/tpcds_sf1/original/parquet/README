query81.sql - field ca_gmt_offset is defined as "double precision" in the database of reference (Greemplum), and as integer in drill view.
              Hence the difference in precision.

              query81.gpm 	- is result genereated by Greenplum.
	      query81.e_tsv 	-- is expected result generated by Drill

query95.sql - looks like a bug in greenplum, I'm checking in Drill result as an expected result for now.

	      query95.e_tsv - is expected result generated by Drill
	      query95.gpm   - is result genereated by Greenplum.

-- incorrect: interval 60 day does not take effect
tpcds1=# SELECT
tpcds1-#          count(*)
tpcds1-# FROM
tpcds1-#          date_dim
tpcds1-# WHERE    d_date BETWEEN '2000-4-01' AND ( Cast('2000-4-01' AS DATE) + INTERVAL '60' day)
tpcds1-# ;
 count
-------
     1
(1 row)

-- Greenplum
tpcds1=# explain SELECT
tpcds1-#          count(*)
tpcds1-# FROM     
tpcds1-#          date_dim 
tpcds1-# WHERE    d_date BETWEEN '2000-4-01' AND ( Cast('2000-4-01' AS DATE) + INTERVAL '60' day);
                                                        QUERY PLAN                                                         
---------------------------------------------------------------------------------------------------------------------------
 Aggregate  (cost=1440.93..1440.94 rows=1 width=8)
   ->  Gather Motion 15:1  (slice1; segments: 15)  (cost=1440.74..1440.91 rows=1 width=8)
         ->  Aggregate  (cost=1440.74..1440.75 rows=1 width=8)
               ->  Seq Scan on date_dim  (cost=0.00..1440.73 rows=1 width=0)
                     Filter: d_date >= '2000-04-01'::date AND d_date <= '2000-04-01 00:00:00'::timestamp without time zone
(5 rows)

-- Greenplum
tpcds1=# select cast('2000-4-01' AS DATE) + INTERVAL '60' day;
      ?column?       
---------------------
 2000-04-01 00:00:00
(1 row)

-- Postgres
postgres=# select cast('2000-4-01' AS DATE) + INTERVAL '60' day;
      ?column?       
---------------------
 2000-05-31 00:00:00
(1 row)


*** Rewritten query returns the same result as drill:
tpcds1=# select
tpcds1-#         count(*)
tpcds1-# from
tpcds1-#         date_dim
tpcds1-# where
tpcds1-#         d_date >= CAST('2000-4-01' AS DATE) and d_date <= CAST('2000-5-31' AS DATE);
 count 
-------
    61
(1 row)


-- correct

0: jdbc:drill:schema=dfs> SELECT
. . . . . . . . . . . . >          count(*)
. . . . . . . . . . . . > FROM
. . . . . . . . . . . . >          date_dim
. . . . . . . . . . . . > WHERE    d_date BETWEEN '2000-4-01' AND ( Cast('2000-4-01' AS DATE) + INTERVAL '60' day)
. . . . . . . . . . . . > ;
+---------+
| EXPR$0  |
+---------+
| 61      |
+---------+
1 row selected (0.477 seconds)

0: jdbc:drill:schema=dfs> explain plan for SELECT
. . . . . . . . . . . . >          count(*)
. . . . . . . . . . . . > FROM     
. . . . . . . . . . . . >          date_dim 
. . . . . . . . . . . . > WHERE    d_date BETWEEN '2000-4-01' AND ( Cast('2000-4-01' AS DATE) + INTERVAL '60' day)
. . . . . . . . . . . . > ;
+------+------+
| text | json |
+------+------+
| 00-00    Screen
00-01      Project(EXPR$0=[$0])
00-02        StreamAgg(group=[{}], EXPR$0=[COUNT()])
00-03          Project($f0=[0])
00-04            SelectionVectorRemover
00-05              Filter(condition=[AND(>=(CAST($0):DATE, CAST('2000-4-01'):DATE NOT NULL), <=(CAST($0):DATE, 2000-05-31 00:00:00))])
00-06                Scan(groupscan=[ParquetGroupScan [entries=[ReadEntryWithPath [path=maprfs:///drill/testdata/tpcds_sf1/parquet/date_dim]], selectionRoot=maprfs:/drill/testdata/tpcds_sf1/parquet/date_dim, numFiles=1, usedMetadataFile=false, columns=[`d_date`]]])
