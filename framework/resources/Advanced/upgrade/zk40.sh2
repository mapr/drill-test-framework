
set -x

if [[ $# -ne 1 ]]
then
  echo zk40.sh2 needs the cluster name
  exit 1
fi

cluster=$1
echo zk40.sh2 cluster $cluster

kubectl get pods -n $cluster

./zkinternal2 $cluster 3 14400
errors=$?

exit $errors

