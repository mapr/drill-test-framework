{
    "testId": "aggregation_group_by_expression",
    "type": "group",
    "description": "Group by expression tests",
    "categories": [ "smoke" ],
    "matrices": [
        {
            "query-file": ".*.sql",
            "schema": "dfs.aggregation",
            "output-format": "tsv",
            "expected-file": ".*.res",
            "verification-type": [
                "in-memory"
            ]
        }
    ],
    "datasources": [
        {
            "mode": "cp",
            "src": "Datasources/aggregation",
            "dest": "aggregation"
        }
    ]
}

