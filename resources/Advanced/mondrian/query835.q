select store.store_city as c0, store.store_postal_code as c1 from store as store where (store.store_state = 'Yucatan') and UPPER(store.store_city) = UPPER('Merida') group by store.store_city, store.store_postal_code order by store.store_city ASC NULLS LAST;
